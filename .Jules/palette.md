## 2024-11-20 - [Area Calculator Error State]
**Learning:** Native `alert()` calls for form validation are disruptive and inaccessible.
**Action:** Replaced `alert()` with conditionally rendered inline error messages using `role="alert"` and styled them consistently with the app theme.
