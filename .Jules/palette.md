## 2026-03-03 - [Missing id and htmlFor in Form Components]
**Learning:** Found pattern of missing `id` attributes on inputs and `htmlFor` on labels in custom form components.
**Action:** Always ensure inputs have `id` attributes and labels have corresponding `htmlFor` attributes for better screen-reader compatibility and mobile usability (click-to-focus).
